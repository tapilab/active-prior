============
Installation
============

At the command line::

    $ easy_install active-prior

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv active-prior
    $ pip install active-prior